{
  "name": "base-fulda",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"php -S localhost:8000\" \"browser-sync start --config bs-config.js\""
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^10.0.5"
  }
}
